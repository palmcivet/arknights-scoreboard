/**
 * @description 计分项类型
 */
export enum SCORING_ITEM_TYPE {
  /**
   * @description 分组
   */
  GROUP = 'group',
  /**
   * @description 勾选
   */
  CHECK = 'check',
  /**
   * @description N 选一
   */
  SELECT = 'select',
  /**
   * @description 按次数
   */
  COUNT = 'count',
}
